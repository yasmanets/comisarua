<div class="col-10 offset-1 policeList">
    <h4>Listado de policías</h4>
    <hr class="rounded">
    {{#if polices}}
    <form action="/police/public" method="POST" enctype="multipart/form-data" class="form-container">
        <p>Selecciona el usuario o usuarios con los que quieres compartir el documento:</p>
        <div class="row checks mb-3">
            {{#each polices}}
            <div class="form-check col-4">
                <input class="form-check-input" type="checkbox" value="{{_id}}" id="policesCheck" name="policesCheck"/>
                <label class="form-check-label" for="policesCheck">{{username}}</label>
            </div>
            {{/each}}
        </div>

        <hr class="rounded">
        <div class="form-controler mb-2">
            <label for="type" class="form-label">Selecciona el tipo de documento que vas a publicar</label>
            <select name="type" id="type" class="form-select form-select-lg" required>
                <option value="payroll" selected>Carta de pago</option>
                <option value="report">Informe</option>
            </select> 
        </div>

        <hr class="rounded">
        <div class="form-row">
            <div class="col-md-12 mb-3">
                <p>
                    Debes adjuntar un documento <b>pdf</b> con información general para el/los usuarios con quién vas
                    compartir el documento.
                </p>
                <input type="file" class="form-control-file" id="file" name="file" accept="application/pdf" required
                    title="El nombre de usuario es obligatorio">
            </div>
        </div>

        <div class="d-flex">
            <button class="btn btn-outline-secondary ml-auto">Publicar</button>
        </div>
    </form>
    {{else}}
    <div class="noPolices">
        <h4>No existen policías registrados en el sistema</h4>
    </div>
    {{/if}}
</div>